# Debug Artifacts

Local screenshots, UI dumps, APK extracts, and similar investigation files belong here.

This directory is ignored by Git except for this README so we can keep the repository root clean
without losing ad-hoc debugging outputs.
